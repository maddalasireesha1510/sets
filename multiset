#include<bits/stdc++.h>
using namespace std;
int main()
{
  multiset<int>ms;//it can store duplicates
  ms.insert(1);
   ms.insert(2);
    ms.insert(2);
    ms.erase(1);//it erase all ones present in set
    ms.erase(ms.find(1));//it 
    
}
